# Package: com.example.scanner.data.source

This package defines the contracts for different data sources, abstracting where the data comes from (e.g., remote API, direct database connection, etc.).

## Contents
- **`DatabaseConnector.kt`:** An interface that serves as a stub for the direct connection to the remote ERP database. It defines methods for executing raw SQL queries.
