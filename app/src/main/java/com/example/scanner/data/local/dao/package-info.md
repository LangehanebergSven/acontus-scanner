# Package: com.example.scanner.data.local.dao

This package contains the Data Access Object (DAO) interfaces for Room.

## Contents
- **DAO Interfaces:** Each interface is annotated with `@Dao` and defines the contract for accessing a specific table in the Room database. They contain methods for `INSERT`, `UPDATE`, `DELETE`, and `SELECT` operations, implemented by Room's code generation.
